name: Link Scanner Pro Daily Run

on:
  schedule:
    # Ye har roz subah 6 baje (UTC) chalega
    - cron: '0 6 * * *'
  workflow_dispatch: # Isse tum manually bhi jab chahe run kar sakte ho

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
      
name: Link Monitor Auto Run

on:
  schedule:
    # Din me 2 baar: 6 AM & 6 PM (IST ≈ UTC 00:30 & 12:30)
    - cron: "30 0,12 * * *"
  workflow_dispatch:

jobs:
  run-monitor:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Install dependencies
        run: npm install

      - name: Run backend script
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          SCRAPER_API_KEY: ${{ secrets.SCRAPER_API_KEY }}
        run: node index.js
        
